# 🔑 Vercel Deployment Variables
# Copy these values to your GitHub repository secrets

# Required for automated deployments
VERCEL_TOKEN=your_vercel_token_here
VERCEL_ORG_ID=your_org_id_here  
VERCEL_PROJECT_ID=your_project_id_here

# Optional - for advanced features
REVALIDATE_TOKEN=your_revalidate_token_here

# Package versions (auto-detected from package.json if not set)
MAIN_VERSION=1.1.0
MCP_VERSION=1.1.0

# 📋 How to get these values:

# 1. VERCEL_TOKEN:
#    - Go to https://vercel.com/account/tokens
#    - Create new token
#    - Copy the value

# 2. VERCEL_ORG_ID & VERCEL_PROJECT_ID:
#    - Run: cd website && vercel env ls
#    - Or check Vercel project settings dashboard

# 3. Add to GitHub Secrets:
#    - Your repo → Settings → Secrets and variables → Actions
#    - Click "New repository secret"
#    - Add each variable above